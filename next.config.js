/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['res.cloudinary.com','marketplace.canva.com,','images.riverisland.com'],
  },
}

module.exports = nextConfig
